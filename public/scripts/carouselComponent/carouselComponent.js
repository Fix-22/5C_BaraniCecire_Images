export const generateCarouselComponent = (parentElement) => {
    let images;

    return {
        build: (inputImages) => {
            images=inputImages;
        },
        render: () => {
            let html=
        }
    }
}